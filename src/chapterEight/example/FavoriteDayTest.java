package chapterEight.example;

public class FavoriteDayTest {
    public static void main(String[] args) {
        FavoriteDay favoriteDay = FavoriteDay.HBD;
        System.out.println(favoriteDay.getName() + "\n" + favoriteDay.getLocation() + "\n" +favoriteDay.getDate());
    }
}
